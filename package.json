{
  "name": "nextcloud-quick-share",
  "version": "1.3.0",
  "private": true,
  "description": "Edge/Chrome 拡張機能。Nextcloud へファイルをアップロードして共有リンクを作る。",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "test:ui": "node test/ui/serve.js"
  }
}
